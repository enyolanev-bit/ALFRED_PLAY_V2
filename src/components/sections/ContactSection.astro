---
/**
 * ContactSection — Section finale après le voyage.
 * CTA contact avec LinkedIn + email. Conclusion naturelle du parcours.
 * Le contenu sera enrichi dans STORY-002.
 */
---

<section class="contact-section" id="contact" aria-label="Contact">
  <div class="contact-section__content">
    <h2 class="contact-section__title">Et maintenant ?</h2>
    <p class="contact-section__text">
      Le voyage à travers l'histoire de l'informatique vous a plu ?<br />
      Parlons de votre prochain projet.
    </p>
    <div class="contact-section__links">
      <a
        href="mailto:contact@nevil.dev"
        class="contact-section__link"
        aria-label="Envoyer un email"
      >
        ✉ Email
      </a>
      <a
        href="https://linkedin.com/in/nevil"
        class="contact-section__link"
        target="_blank"
        rel="noopener noreferrer"
        aria-label="Profil LinkedIn"
      >
        → LinkedIn
      </a>
    </div>
  </div>
</section>

<style>
  .contact-section {
    position: relative;
    width: 100%;
    min-height: 100vh;
    min-height: 100dvh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 1.5rem;
    text-align: center;
    background: linear-gradient(180deg, #0a0a1a 0%, #0a0a0a 100%);
  }

  .contact-section__content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
    max-width: 30rem;
  }

  .contact-section__title {
    font-family: var(--font-display);
    font-size: clamp(2rem, 6vw, 3.5rem);
    font-weight: 700;
    line-height: 1.1;
  }

  .contact-section__text {
    font-size: clamp(1rem, 2.5vw, 1.125rem);
    opacity: 0.7;
    line-height: 1.7;
  }

  .contact-section__links {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    justify-content: center;
    margin-top: 1rem;
  }

  .contact-section__link {
    padding: 0.875rem 2rem;
    font-family: var(--font-body);
    font-size: 1rem;
    font-weight: 600;
    color: #fff;
    text-decoration: none;
    border: 2px solid rgba(255, 255, 255, 0.3);
    border-radius: 9999px;
    transition: border-color 0.2s ease, background 0.2s ease;
  }

  .contact-section__link:hover {
    border-color: var(--color-accent);
    background: rgba(233, 69, 96, 0.1);
  }
</style>
